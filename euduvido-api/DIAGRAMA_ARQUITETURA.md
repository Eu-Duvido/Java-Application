# Diagrama de Arquitetura - Eu Duvido API

## 🏗️ Visualização das Camadas

```
┌────────────────────────────────────────────────────────────────┐
│                     CLIENTE MOBILE                              │
│                  (iOS, Android, etc)                            │
└────────────────────────┬─────────────────────────────────────────┘
                         │
                         │ HTTP REST
                         │
┌────────────────────────▼─────────────────────────────────────────┐
│                                                                  │
│            ENTRYPOINT / PRESENTATION LAYER                       │
│                  (Controllers, DTOs)                             │
│                                                                  │
│  ┌──────────────┐ ┌──────────────┐ ┌────────────────────┐      │
│  │    User      │ │  Challenge   │ │  Participation &   │      │
│  │ Controller   │ │ Controller   │ │   Proof Controller │      │
│  └──────┬───────┘ └──────┬───────┘ └────────┬───────────┘      │
│         │                │                  │                   │
│  ┌──────▼────────────────▼──────────────────▼────────────────┐  │
│  │     DTOs: Request e Response                             │  │
│  │  - CreateUserRequest    - UserResponse                   │  │
│  │  - CreateChallengeRequest - ChallengeResponse            │  │
│  │  - SubmitProofRequest   - ProofResponse                  │  │
│  └──────┬──────────────────────────────────────────────────┬──┘  │
│         │                                                   │     │
└─────────┼───────────────────────────────────────────────────┼─────┘
          │ (depende de)                                      │
          │                                                   │
┌─────────▼───────────────────────────────────────────────────▼─────┐
│                                                                  │
│          APPLICATION / USE CASES LAYER                           │
│                                                                  │
│  ┌────────────────────┐  ┌────────────────────┐                │
│  │ CreateUserUseCase  │  │CreateChallengeUseCase               │
│  └────────────────────┘  └────────────────────┘                │
│                                                                  │
│  ┌────────────────────┐  ┌────────────────────┐                │
│  │AcceptChallengeUseCase │ RefuseChallengeUseCase              │
│  └────────────────────┘  └────────────────────┘                │
│                                                                  │
│  ┌────────────────────┐  ┌────────────────────┐                │
│  │ SubmitProofUseCase │  │ApproveProofUseCase │                │
│  └────────────────────┘  └────────────────────┘                │
│                                                                  │
│  ┌─────────────────────────┐  ┌──────────────────────────┐     │
│  │ListCreatedChallengesUseCase│ListReceivedChallengesUseCase│  │
│  └─────────────────────────┘  └──────────────────────────┘     │
│                                                                  │
│  ┌────────────────────────────────────────────────────────┐    │
│  │  UpdateExpiredChallengesUseCase                        │    │
│  └────────────────────────────────────────────────────────┘    │
│                                                                  │
└─────────────┬────────────────────────────────────┬──────────────┘
              │ (depende de)                        │
              │                                     │
┌─────────────▼─────────────────────────────────────▼──────────────┐
│                                                                  │
│            DOMAIN LAYER                                          │
│        (Entidades, Enums, Interfaces)                            │
│                                                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │    User      │  │  Challenge   │  │ChallengeParti│          │
│  │  (Entity)    │  │  (Entity)    │  │cipation(Entity)          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
│                                                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │    Proof     │  │ChallengeStatus│  │Participation│          │
│  │  (Entity)    │  │   (Enum)     │  │Status(Enum) │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
│                                                                  │
│  ┌──────────────────────────────────────────────────────┐      │
│  │  Repository Interfaces (Contratos)                   │      │
│  │  - UserRepository                                    │      │
│  │  - ChallengeRepository                               │      │
│  │  - ChallengeParticipationRepository                  │      │
│  │  - ProofRepository                                   │      │
│  └──────────────────────────────────────────────────────┘      │
│                                                                  │
│  ✅ SEM SPRING, SEM JPA, APENAS LÓGICA DE NEGÓCIO               │
│                                                                  │
└─────────────┬────────────────────────────────────┬──────────────┘
              │ (implementado por)                  │
              │                                     │
┌─────────────▼─────────────────────────────────────▼──────────────┐
│                                                                  │
│        INFRASTRUCTURE LAYER                                      │
│      (Spring Data JPA, Persistência)                             │
│                                                                  │
│  ┌───────────────────────────────────────────────────────┐     │
│  │  Repository Implementations (Spring @Component)        │     │
│  │  - UserRepositoryImpl                                  │     │
│  │  - ChallengeRepositoryImpl                             │     │
│  │  - ChallengeParticipationRepositoryImpl                │     │
│  │  - ProofRepositoryImpl                                 │     │
│  └──────┬────────────────────────────────────────────────┘     │
│         │                                                        │
│  ┌──────▼───────────────────────────────────────────────┐      │
│  │  Spring Data JPA Repositories                        │      │
│  │  - UserJpaRepository extends JpaRepository           │      │
│  │  - ChallengeJpaRepository extends JpaRepository       │      │
│  │  - ChallengeParticipationJpaRepository               │      │
│  │  - ProofJpaRepository extends JpaRepository           │      │
│  └──────┬───────────────────────────────────────────────┘      │
│         │                                                        │
│  ┌──────▼───────────────────────────────────────────────┐      │
│  │  JPA Entities (Mapeamento ORM)                       │      │
│  │  - UserEntity (@Entity, @Table)                      │      │
│  │  - ChallengeEntity (@Entity, @Table)                 │      │
│  │  - ChallengeParticipationEntity (@Entity)            │      │
│  │  - ProofEntity (@Entity, @Table)                     │      │
│  └──────┬───────────────────────────────────────────────┘      │
│         │                                                        │
│  Mappers:                                                       │
│  - UserEntity.fromDomain(user)  →  UserEntity                  │
│  - UserEntity.toDomain()        →  User                        │
│                                                                  │
└─────────────┬────────────────────────────────────────────────────┘
              │ (persiste em)
              │
┌─────────────▼──────────────────────────────────────────────────┐
│                                                                │
│                    DATABASE LAYER                              │
│                  (H2 em desenvolvimento)                        │
│                  (PostgreSQL em produção)                       │
│                                                                │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐        │
│  │    users     │  │  challenges  │  │challenge_    │        │
│  │              │  │              │  │participations         │
│  └──────────────┘  └──────────────┘  └──────────────┘        │
│                                                                │
│  ┌──────────────┐                                             │
│  │    proofs    │                                             │
│  │              │                                             │
│  └──────────────┘                                             │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Fluxo de Requisição Detalhado

### Exemplo: Criar Usuário (POST /api/v1/users)

```
1. HTTP REQUEST
   ┌──────────────────────────┐
   │ POST /api/v1/users       │
   │ Content-Type: JSON       │
   │ {                        │
   │   "name": "João",        │
   │   "email": "joao@...",   │
   │   "password": "senha123" │
   │ }                        │
   └──────────┬───────────────┘
              │
2. ENTRYPOINT LAYER
   ┌──────────────────────────┐
   │ UserController           │
   │ @PostMapping("/"")       │
   │                          │
   │ 1. Recebe JSON           │
   │ 2. Spring desserializa   │
   │    CreateUserRequest     │
   └──────────┬───────────────┘
              │
3. VALIDAÇÃO (BEAN VALIDATION)
   ┌──────────────────────────┐
   │ @Valid                   │
   │ @NotBlank, @Email        │
   │ @Size, etc               │
   │                          │
   │ ✓ Se válido              │
   │ ✗ Se inválido → 400      │
   └──────────┬───────────────┘
              │
4. CALL USE CASE
   ┌──────────────────────────┐
   │ CreateUserUseCase        │
   │ .execute(name, email,    │
   │          password, url)  │
   └──────────┬───────────────┘
              │
5. APPLICATION LAYER
   ┌──────────────────────────┐
   │ CreateUserUseCase        │
   │                          │
   │ 1. Verifica email único  │
   │    userRepository        │
   │    .existsByEmail()      │
   │                          │
   │ 2. Se existe → erro 400  │
   │ 3. Se não existe:        │
   │    User.create(...)      │
   │    (factory method)      │
   └──────────┬───────────────┘
              │
6. DOMAIN LAYER
   ┌──────────────────────────┐
   │ User.create(...)         │
   │                          │
   │ 1. Valida dados:         │
   │    - nome não vazio      │
   │    - email com @         │
   │    - senha >= 6 chars    │
   │                          │
   │ 2. Se válido:            │
   │    new User(...)         │
   │ 3. Se inválido → erro    │
   └──────────┬───────────────┘
              │
7. USE CASE (CONTINUA)
   ┌──────────────────────────┐
   │ CreateUserUseCase        │
   │                          │
   │ userRepository.save(user)│
   └──────────┬───────────────┘
              │
8. INFRASTRUCTURE LAYER
   ┌──────────────────────────┐
   │ UserRepositoryImpl        │
   │ .save(user)              │
   │                          │
   │ 1. Converte domain       │
   │    UserEntity.fromDomain │
   │                          │
   │ 2. Chama JPA             │
   │    jpaRepository.save()  │
   └──────────┬───────────────┘
              │
9. JPA/SPRING DATA
   ┌──────────────────────────┐
   │ UserJpaRepository        │
   │ extends JpaRepository    │
   │                          │
   │ 1. Gera SQL              │
   │ 2. Executa INSERT        │
   │ 3. Retorna entidade      │
   │    persistida            │
   └──────────┬───────────────┘
              │
10. DATABASE
    ┌──────────────────────────┐
    │ INSERT INTO users(...)   │
    │ RETURNING id             │
    │                          │
    │ users table:             │
    │ id | name | email | ...  │
    │ 1  | João | joao@ | ...  │
    └──────────┬───────────────┘
              │
11. BACK TO INFRASTRUCTURE
    ┌──────────────────────────┐
    │ UserRepositoryImpl        │
    │                          │
    │ UserEntity.toDomain()    │
    │ → User (domínio)         │
    └──────────┬───────────────┘
              │
12. BACK TO CONTROLLER
    ┌──────────────────────────┐
    │ UserController           │
    │                          │
    │ UserResponse.fromDomain()│
    │ → UserResponse (DTO)     │
    └──────────┬───────────────┘
              │
13. HTTP RESPONSE
    ┌──────────────────────────┐
    │ HTTP 201 Created         │
    │ Content-Type: JSON       │
    │ {                        │
    │   "id": 1,              │
    │   "name": "João",       │
    │   "email": "joao@...",  │
    │   "createdAt": "..."    │
    │ }                        │
    └──────────┬───────────────┘
              │
14. CLIENTE RECEBE
    └──────────────────────────┘
```

---

## 📊 Diagrama de Entidades

```
                    USER
        ┌──────────────────────┐
        │ - id: Long (PK)      │
        │ - name: String       │
        │ - email: String (UK) │
        │ - password: String   │
        │ - profileImageUrl    │
        │ - createdAt          │
        └──────────────────────┘
           ▲          ▲
           │          │
           │          └─────────────┐
           │                        │
           │                     CHALLENGE
           │        ┌──────────────────────────┐
           │        │ - id: Long (PK)          │
           │        │ - title: String          │
           │        │ - description: String    │
           │        │ - creator_id (FK)──────┘
           │        │ - deadline: DateTime     │
           │        │ - status: ChallengeStatus│
           │        │ - locationRequired: Bool │
           │        │ - createdAt              │
           │        └────────────┬─────────────┘
           │                     │
           │                     │ 1:N
           │        ┌────────────▼──────────────────────┐
           │        │ CHALLENGE_PARTICIPATION           │
           │        │ - id: Long (PK)                   │
           │        │ - user_id (FK)────────────┐       │
           │        │ - challenge_id (FK)───────┤──┐    │
           │        │ - status: ParticipationStatus     │
           │        │ - createdAt                       │
           │        └────────────┬────────────────────┘
           │                     │
           │                     │ 1:N
           │        ┌────────────▼──────────────────────┐
           │        │ PROOF                             │
           │        │ - id: Long (PK)                   │
           │        │ - participation_id (FK)──────────┘
           │        │ - mediaUrl: String                │
           │        │ - mediaType: MediaType            │
           │        │ - latitude: Double                │
           │        │ - longitude: Double               │
           │        │ - submittedAt: DateTime           │
           │        │ - approved: Boolean               │
           │        └───────────────────────────────────┘
           │
           └─────── 1:N
```

---

## 🔐 Fluxo de Dependência

```
┌─────────────────────────────────────────────┐
│  EXTERNAL (HTTP, Clients, Mobile)           │
└────────────────┬────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────┐
│  ENTRYPOINT                                 │
│  (Controllers, DTOs, Validation)            │
│  ✓ Depende de: Application                  │
│  ✗ Depende de: Database                     │
└────────────────┬────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────┐
│  APPLICATION                                │
│  (Use Cases)                                │
│  ✓ Depende de: Domain                       │
│  ✗ Depende de: HTTP, Database               │
└────────────────┬────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────┐
│  DOMAIN ⭐ PURO                              │
│  (Entities, Enums, Interfaces)              │
│  ✗ Depende de: NADA!                        │
│  (Testável em isolamento)                   │
└────────────────┬────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────┐
│  INFRASTRUCTURE                             │
│  (JPA, Repositories, Spring Data)           │
│  ✓ Implementa: Domain                       │
│  ✓ Depende de: Database                     │
└────────────────┬────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────┐
│  DATABASE                                   │
│  (Tables, Queries, Constraints)             │
└─────────────────────────────────────────────┘
```

**REGRA DE OURO**: Dependências sempre apontam para DENTRO (para o Domain)

---

## 🧩 Padrões Utilizados

### 1. Factory Pattern (Domain)
```
User user = User.create(name, email, password, profileImageUrl)
                 ▲
                 │
            Valida dados
            Cria entidade
            Imutável
```

### 2. Repository Pattern (Domain → Infrastructure)
```
Domain:         UserRepository (Interface)
                     ▲
                     │ implementa
                     │
Infrastructure: UserRepositoryImpl (Implementação)
                     ▲
                     │ usa
                     │
                UserJpaRepository (Spring Data)
```

### 3. DTO Pattern (Entrypoint)
```
HTTP Request    →   CreateUserRequest DTO
                    ↓ Validação
                    ↓ Mapeamento
Domain Entity:  User (nunca exposto)
                    ↓ Conversão
HTTP Response   ←   UserResponse DTO
```

### 4. Adapter Pattern (Infrastructure)
```
Domain User ←──────→ UserEntity (JPA)
              fromDomain()
              toDomain()
```

---

## 📈 Escalabilidade

### Adicionar Nova Feature

Exemplo: Adicionar "Curtidas" em desafios

```
1. DOMAIN
   ├── entities/Like.java (nova entidade)
   └── repositories/LikeRepository.java (nova interface)

2. APPLICATION
   └── usecases/LikeChallengeUseCase.java (novo caso de uso)

3. INFRASTRUCTURE
   ├── persistence/entities/LikeEntity.java
   ├── persistence/repositories/LikeJpaRepository.java
   └── repositories/LikeRepositoryImpl.java

4. ENTRYPOINT
   ├── controllers/LikeController.java
   └── dtos/LikeResponse.java

5. CONFIG
   └── UseCaseConfig.java (adicionar bean)
```

**A arquitetura permite adicionar features sem quebrar código existente!**

---

## ✅ Checklist de Qualidade

- ✅ Domain isolado (sem frameworks)
- ✅ Factory methods (criação segura)
- ✅ Interfaces de repositório (inversão de controle)
- ✅ DTOs em entrada/saída (nunca expõe domain)
- ✅ Validação em múltiplos níveis (DTO + Domain)
- ✅ Tratamento de exceções centralizado
- ✅ Logs estruturados
- ✅ CORS configurado
- ✅ Banco de dados isolado
- ✅ Documentação completa
- ✅ Testes unitários
- ✅ Código limpo e legível

---

**Arquitetura robusta, escalável e profissional! 🚀**

